# Contributing to PH Launcher

感谢你帮助改进 PH Launcher。这个项目会接触学校网站、学生计划与可选 AI，因此隐私和安全优先于功能数量。

## 开始前

1. 使用 Node.js 22 和 `npm ci` 安装锁定依赖。
2. 按 README 的步骤从 ECDICT 生成本地词典数据库。
3. 从小而明确的 issue 或改动开始；一个提交尽量只解决一个问题。

## 提交要求

- 不提交真实学校账号、密码、Cookie、验证码、API Key、学生姓名、课表截图或作业内容。
- 不提交 `ecdict.db`、模型、安装包、缓存、日志、证书、私钥或用户数据。
- 学校网站自动化必须使用明确的域名和操作白名单，保持 `contextIsolation`、`sandbox` 与 `webSecurity` 开启。
- AI 写入必须先生成可读预览，并在用户明确确认后执行；不要增加静默写入、删除、发信或提交作业能力。
- 不要为解决兼容性问题关闭 TLS、CSP、签名校验或操作系统安全保护。
- 用户可见页面只写最终用户需要的内容；实现过程和评审说明放在开发文档或 PR 描述中。

## 验证

```powershell
npm test
npm run self-test
npm run smoke
```

涉及学校网站时，还应在不暴露账号信息的前提下人工验证登录、导航、下载、弹窗与“简洁显示”开关。涉及安装包时应核对 SHA-256，并完成安装与卸载测试。

## Pull Request

请说明问题、根因、最小改动、风险和验证结果。不要在 issue、PR、日志或截图中粘贴敏感数据。

