# PH Launcher 0.5.1 发布与安全说明

## 本版更新

- IB 指令词改为按科目和考试版本查询，并显示对应 AO。历史课分别提供“考试至 2027”和“首考 2028”，避免不同课程版本混用。
- 每个已收录科目都采用独立词表，覆盖 Language A、Language B、ab initio、数学 AA／AI、科学、人文、计算机科学与视觉艺术；可直接打开对应的 IB 官方依据。中文答题提示为原创学习辅助，最终以任课教师和当前学科指南为准。
- 增加 IB 官方样卷入口，以及带明确风险提示的 IB Docs 第三方外部入口。
- 改进三个学校网站允许保留的登录数据写入，减少意外退出后的登录丢失；实际登录有效期仍由原网站决定。
- “简洁显示”现在默认关闭；开启后会区分“已应用”和“当前页不可用”，并继续提供一键关闭与原网页回退。旧版本升级时会一次性恢复为关闭，之后保留用户自己的选择。
- 增加“我的网页”：可添加最多 12 个 HTTPS 网站，设置颜色、顺序和独立全局快捷键。每个网站使用独立登录空间；换域名、删除或手动清理时会关闭相关窗口并清除该网站的数据，下载前会询问保存位置。
- 修复 Windows 任务栏小尺寸图标可能空白的问题。
- Mac 安装盘更清楚地区分 App 与桌面安装盘，并提供拖入“应用程序”及复制粘贴两种安装方式。

## IB Docs 使用边界

IB Docs 是第三方网站，与 International Baccalaureate Organization（IBO）无隶属或背书关系，可能包含受版权保护的资料。PH Launcher 只在系统浏览器中打开该网站，不内嵌、下载、缓存、镜像、索引或代理其中内容，也不共享学校网站的登录状态。

仅在学校或相关权利人明确授权的情况下访问和使用资料。查询课程结构与练习材料时，建议优先使用 PH Launcher 提供的 IB 官方课程页和官方样卷入口。

## 登录、AI 与数据安全

三个学校网站使用彼此隔离的持久登录空间。PH Launcher 不读取、备份或改写网站密码、Cookie 与验证码；各网站仍可按照自己的安全策略要求重新登录。

AI 默认关闭。“AI 操作启动器”开启后，读取与写入权限仍受本地授权控制；所有任务、笔记和课表更改必须先预览，再由用户确认。AI 不能读取密码、Cookie、验证码、API Key，不能发送邮件、提交作业或清除登录数据。

AI 可以按名称打开用户已经添加的网站，但不能读取自选网页内容、填写表单或接受任意网址。自选网站只允许 HTTPS，主进程会校验网址和 ID；每个网站使用单独的持久登录分区，并默认拒绝摄像头、麦克风、定位等网页权限。

## 分发说明

- Windows 安装版和免安装版目前没有商业代码签名证书，系统可能显示“未知发布者”；请从项目 GitHub Release 下载并核对 SHA-256。
- macOS 测试版是 Universal 应用，支持 Apple 芯片与 Intel Mac，但只有 ad-hoc 临时签名，未经 Developer ID 签名和 Apple 公证。它不是正式版，请从完整预发布页下载、阅读风险提示并核对 SHA-256。
- 不要使用关闭 Gatekeeper、移除隔离属性或重新签名等方式绕过 macOS 安全检查。

PH Launcher 是独立学生工具，不是上海市民办平和学校、ManageBac、EduPage、网易、IBO 或 IB Docs 的官方产品，也不使用校徽。
