# PH Launcher 0.5.0 发布与安全说明

> 当前状态：0.5.0 源码、测试和自动构建流程已经同步，但尚未发布可供同学安装的 0.5.0 正式包。稳定下载仍为 Windows v0.4.1；Mac CI 产物未签名，只用于开发验证。

## 本版重点

- 增加 macOS Universal PKG、DMG 与 ZIP 构建链，正式版支持 Developer ID 签名、Apple 公证和 Gatekeeper 验证。
- Mac 本地 AI 支持一键部署：下载固定版本的 Ollama 官方 DMG，核对固定 SHA-256、应用标识、Apple Team ID、代码签名和公证结果后再安装。
- 完善下载断点缓存绑定、大小限制、挂载设备追踪、进程超时收敛和本机监听身份检查。
- “关于”页面版本号由应用自身读取，不再手工写死。
- 离线词典数据库不直接提交到 Git；构建时从固定 ECDICT 提交下载 CSV、核对 SHA-256 后生成，正式安装包仍完整离线使用。

## AI 操作安全边界

“AI 操作启动器”默认关闭。开启前会显示风险提示；开启后，AI 可按授权读取任务、常规课程表、少量笔记摘要、今日概览、离线词典与 IB 指令词，并提出新增任务、笔记或课程等更改。

所有写入先生成完整预览，只有用户确认后才保存；预览后数据发生变化时，旧确认自动失效。PH Launcher 不向 AI 提供网站密码、Cookie、验证码、API Key、任意文件系统或任意网页脚本权限。AI 不能清除登录数据、删除资料、发送邮件或提交作业。

EduPage 只读取用户已打开的“常规课表”中的课程名、星期、时间与教室。动态调课、停课和今日视图不会作为固定课表导入，空结果也不会删除已有课程。

## 本地 AI 安全边界

本地服务只允许绑定 `127.0.0.1` 或 `::1`。如果 11434 端口由其他程序占用、对局域网开放，或监听进程不是已验证的 Ollama，部署会停止且不会结束未知进程。取消部署时，已独立启动的 Ollama 可能继续在后台运行，界面会明确说明这一状态。

## 分发要求

- Windows 文件目前没有商业代码签名证书，系统可能显示“未知发布者”；请通过项目 GitHub Release 获取并核对 SHA-256。
- Mac 正式包必须同时通过 Developer ID、Apple 公证、Gatekeeper、Universal 架构和实机安装验证；未签名测试包不得直接分享给同学。
- Mac Hardened Runtime 仅开放 JIT 权限，不允许未签名可执行内存，也不关闭动态库校验。
- 项目使用 MIT 许可证。离线词典数据来自 MIT 许可的 ECDICT，并保留第三方许可声明。

PH Launcher 是独立学生工具，不是上海市民办平和学校、ManageBac、EduPage、网易或 Ollama 的官方产品，也不使用校徽。
