# PH Launcher

桌面学习启动器，入口 electron/main.cjs；前端 src/，回归测试 tests/。

工作约定：先阅读调用链再修改；保存已有学习记录；新界面兼顾中文与英文。学校账号变更必须清除对应缓存。API 授权应明确范围，可撤销，并绑定当前连接。仅使用合成数据测试，不把用户凭据写入代码、测试或文档。

当前工作：词本分类、逐词保存、离线课表、首次使用导览与常用操作便捷性。Mac 工作由用户同学处理。

验证：npm test；需要打包时另行核对版本与产物。交接状态见 handoff.md。
