# PH Launcher 使用指南

PH Launcher 是面向平和 IB 学生的桌面学习工作台。它把学校邮箱、ManageBac、EduPage、计划、笔记、课程提醒、专注计时、背单词、IB 小工具和可选 AI 放在一个安静、统一的界面中。当前协作预览版本为 `1.0.0`，组合发行版采用 GPL-3.0-or-later。

> PH Launcher 不是上海市民办平和学校、ManageBac、EduPage 或网易的官方产品，也不使用校徽。网站名称与服务归各自权利人所有。

## 开始使用

1. 使用开发者分享的原始安装包，或从项目 GitHub Releases 页面选择实际存在的版本，并核对随包校验清单。Windows Folder ZIP 请完整解压到一个文件夹后再运行，它不是 Portable 自解压程序。
2. `1.0.0` 提供 Windows x64 安装版／文件夹版与 Mac Universal 体验包。Mac 支持 Apple 芯片及 Intel，要求 macOS 13+（本地 AI 一键部署需 macOS 14+）。Mac 尚无 Developer ID 身份签名与 Apple 公证：打开 DMG，将 App 拖入 Applications 后首次打开，若系统拦截，在“系统设置 → 隐私与安全性”核实来源后选择“仍要打开”。不要关闭系统安全功能。
3. 如果 Windows SmartScreen 提示“未知发布者”，请先确认文件来源与校验值，再选择“更多信息 → 仍要运行”。Windows 版目前未购买代码签名证书。
4. 在左侧打开平和邮箱、ManageBac 或 EduPage。ManageBac／EduPage 可在“设置 → 网站 → 账号记忆”中输入账号密码后选择“保存并登录”；其他常用学习网站可稍后在“我的网页”中添加。

ManageBac 与 EduPage 的账号记忆使用当前系统用户的系统密钥加密保存。“保存并登录”只完成本次登录；已保存账号可在启动后后台同步。“自动重新登录”是另一项独立选项，默认关闭。它只会在读取操作登录失效时按受限流程尝试一次，不会处理验证码或绕过双重验证。平和邮箱也使用系统加密凭据库，但不会自动填入网页，并使用客户端授权码连接。保存内容不会进入 AI 或导出备份；共享电脑不要启用。

设置中的“清除登录数据”会清除对应网站的 Cookie、缓存与登录状态，不影响 PH Launcher 里的笔记和任务。

更新前请从系统托盘菜单选择“退出”，再打开新版本；仅关闭窗口可能仍让旧版在后台运行。Folder ZIP 请解压到新文件夹，保留全部配套文件，不要单独复制 EXE。已有的本机学习数据和手动设置的字号会保留。

## 我的网页

在侧栏“我的网页”旁点击 `＋`，或进入“设置 → 网站 → 我的网页”，填写名称与 HTTPS 地址即可添加。每个自选网站都有独立登录空间，不会和学校邮箱、ManageBac、EduPage 或其他自选网站共享登录信息；最多可添加 12 个，并可调整颜色、顺序和自己的全局快捷键。

只允许添加 HTTPS 网站。修改同一网站的页面路径会保留登录，换成另一个域名会自动清除旧站登录数据；删除网站或点击“清除登录”也会同时清理该网站的数据。网站发起文件下载时，PH Launcher 会先让你选择保存位置。请只添加自己信任的网站，并在显示的地址或登录弹窗标题中核对域名。

## 学习工具

- 计划：建立任务、截止时间、科目、优先级与预计用时。
- 课程表：按星期添加课程、教室和提前提醒时间。提醒要求 PH Launcher 正在运行或停留在系统托盘；如需每天自动提醒，可在设置中开启开机启动。
- 专注计时：提供 25/5、45/10、50/10 和正计时模式，也可自定 1–180 分钟。点击侧栏“调整时长与目标”，填写本轮目标并选择学习页面或已添加的网站；开始或继续时会打开目标。侧栏和专注页均可暂停、继续或结束。本轮运行中修改时长从下一轮生效；提前结束不会算作完成。
- 笔记：自动保存，支持搜索、科目、置顶，并可把笔记转为任务。
- 离线英汉词典：内置 770,611 个本地词条，支持音标、中英文释义、词形变化、宽松匹配、系统语音朗读及保存到笔记；查询内容不会联网发送。词库来自 MIT 许可的 [ECDICT](https://github.com/skywind3000/ECDICT)。
- IB 工具：“按科目查指令词”可先选择 Language A、数学 AA／AI、各科学与人文科目，再查询 Analyse、Evaluate、Discuss 等题目动词要求的答题动作、所属 AO 与深度；它不是 AI 指令。每项会标明适用的首考或最后考试年份，历史课分别提供“考试至 2027”和“首考 2028”，请按自己的课程版本选择。中文说明是原创学习提示，最终以任课教师和当前学科指南为准。另含中英文写作统计、加权成绩计算，以及 EE、TOK、IA 里程碑模板。
- 学习资源：IB 官方课程页、官方样卷和 IB Docs 都会在系统浏览器中打开。IB Docs 是第三方网站，与 IBO 无隶属或背书关系，可能包含受版权保护的资料；PH Launcher 不内嵌、不下载、不缓存或索引其中内容。仅在学校或权利人明确授权的情况下访问和使用。

### 背单词与阅读

- “今日学习”开始本轮学习，“我的词书”管理词本与词条，“更多工具”提供阅读积累、手动收词和导入备份。复习使用本地 FSRS 6 排期，内置 `original36` 原创 36 词入门包，也可从离线词典选择词本或导入自己的词汇。
- 新词按当天稳定打乱的顺序，每组最多 5 个。先逐个阅读词义和完整例句，看完这组后再统一回忆。已有到期复习时会先复习，到期数量也会显示在侧栏黄色提示中；没有到期词时，主按钮是“开始学新词”。
- 点击“我会这个词”会跳过并暂停该词，不会计为已经成功复习；需要重新学习时，在“我的词书”的已暂停列表中恢复。
- 可在语境回忆、词义回忆和拼写之间练习，评分后可撤销，并可暂停暂时不想复习的词。
- 在“自己的表达”中写句子后点击“保存表达”，以后仍可查看和修改。启用本地 AI 后可点击纠错；只把当前词和句子送到本机模型，不发送给云端，也不会自动替换你的表达。“采用建议”后仍需点击保存。AI 可能判断错误，请自行核对。
- “阅读积累”可保存本人有权使用的文本，在原句中查词、标记和收藏上下文。阅读材料、上下文、复习与阅读记录只保存在本机。
- 只有点击“我已读完这篇”后，阅读词次和用时才会记入统计；这些数字是本人确认的阅读记录，不是理解程度或词汇量测评。
- “我的词书”提供 TOEFL、IELTS、CET、GRE、高频阅读等参考词本，可从内置 ECDICT 离线分批加入；不是官方考试词表。另有 60 词原创语境练习本，全部带填空例句。自评和简短测验只建议粗略起点；“今日学习”可另选本地 AI（默认）或经同意的 API，根据难度与近期学习记录推荐下一组，不可用时离线继续。视频方法说明依据用户提供的摘要，并非完整字幕；来源见 [研究说明](docs/vocabulary-research.md)。
- 在阅读正文中用鼠标选中英文单词，可离线查词并自动收录单词和原句；不会重置重复词的复习进度。

### 学校应用五个入口

左侧依次为“我的课表、我的日程、班级课表、我的课程、平和邮箱”。ManageBac／EduPage 可在“设置 → 网站 → 账号记忆”输入账号密码、确认风险后直接“保存并登录”，无需先打开原网页登录。已保存账号可在启动后后台同步。学校页面的同步只读取数据，不会向学校网站写入；提交作业、回复讨论与发信只会在你确认 AI 的更改清单后发生。自动重新登录仍须单独开启，默认关闭。

- EduPage 课表默认显示周一至周五，可点“显示周末”。尚未选择教学组时会提示下一步，可参考已同步课程的唯一匹配建议；不能确定时需本人确认。冲突课程聚合显示。选择教学组后，可设置上课提醒或导入到本地计划；停课不导入，未覆盖日期不误显示为“无课”。导入的是具体日期课程，不是每周循环课程。
- ManageBac 可显示课程、明确标注的课程总评、作业及详情、课程单元、课程文件名称、课程日历，以及 CAS／EE 摘要；Discussion 页面只读显示列表、正文和已有回复，不会在其中直接发布内容（回复需经 AI 清单确认）。Discussion 尚未完成真实账号页面验证，如有差异请回到原网页核对。
- EduPage 的授权登录与整周课表读取，以及 ManageBac 的真实授权登录和课程列表读取均已完成验证；不会保存或展示任何验证账号、密码或课程内容。网站改版、权限差异或登录过期都可能导致缺项；所有成绩、截止日期和课程安排必须回到原网页核对。
- “班级课表”展示当前账号所属班级的全部可见教学组，不受个人选课影响；暂不支持切换到任意其他班级。
- “平和邮箱”显示最近 100 封邮件，支持全部／未读筛选、纯文本正文、附件标识、正文前的附件保存、从已读取邮件头中补全联系人和写邮件。打开一封未读邮件会在服务器上标记为已读。邮件按钮与链接另列在正文前，显示实际域名；点击并确认后交给系统浏览器打开，不自动加载图片或访问链接。手工写信与 AI 发信都会在发送前由系统确认；附件和链接流程仅以测试邮件数据核对，尚未完成真实收发验证。暂不支持发送附件。
- 自动更新默认关闭。首次手动读取后，可在课表或课程页面中开启；仅当页面可见、每五分钟检查且结果过期时才会读取。结果只在本次运行的内存中保留。

### 我的日程与外观

“我的日程”提供周、月、年视图，点击日期可新增或编辑本地日程，并单独设置准时或提前提醒。全天日程需要先填写起止时间才能开启提醒。“我的课表 → 上课提醒”独立控制已选教学组的课程提醒；原“计划”中手动课程不受影响。专注结束、上课和日程到时使用独立大弹窗，可关闭或延后 5 分钟。电脑须唤醒、程序须运行；学校课表须已成功同步。

设置中可选择六套主题、四种角色配色，并将全局字号在 14–24px 间调整，默认 16px。已有手动字号不会被覆盖。主题同时影响顶部背景和 Windows 窗口按钮区域，重启后保持。

“设置 → 通用 → 界面语言”可选择简体中文或 English，立即生效并保存。只切换界面：学校内容、阅读材料、词义、笔记、目标和 AI 回复保持原文；系统文件选择器的语言由系统控制。

## 快捷键

窗口内默认可用；Mac 请把下列 `Ctrl` 换成 `⌘`：

- `Ctrl+1 / 2 / 3`：学校邮箱 / ManageBac / EduPage
- `Ctrl+K`：命令面板
- `Ctrl+D`：离线词典
- `Ctrl+Shift+N`：新建笔记
- `Ctrl+Shift+A`：新建任务
- `Ctrl+Shift+P`：开始或暂停专注
- `Ctrl+,`：设置

Windows 默认全局快捷键为 `Ctrl+Alt+Space`，Mac 为 `⌘+Shift+Space`，用于显示或隐藏 PH Launcher。其他全局快捷键可在设置中按需开启、修改；若组合键已被其他软件占用，PH Launcher 会给出提示。

## 可选 AI

AI 默认关闭。关闭时，三个学校网站入口和全部本地工具仍可完整使用，也不会下载模型或连接 AI 服务。

### 本地 AI

每台电脑进入“AI 学习助手 → 本地 AI”时，PH Launcher 都会重新读取本机处理器、内存、显卡显存和系统盘可用空间，再给出该电脑自己的建议。更换电脑后不会沿用另一台电脑的推荐。

点击“一键部署推荐模型”后，PH Launcher 会查找可信的现有 Ollama；如未安装，则从 Ollama 官方来源下载安装包，核对官方 SHA-256 与平台数字签名，随后启动本地服务、下载推荐模型、验证并自动启用。Windows 会核对 Authenticode 发布者；Mac 会核对固定的应用标识、Apple Developer ID 团队和 Gatekeeper 公证结果。页面会显示进度，下载过程中可以取消。安装包支持自动重试与断点续传；失败原因会直接显示，同时保存在不含学习资料和账号信息的本地部署日志中。

Apple 芯片 Mac 会根据统一内存与磁盘空间给出保守建议。Intel Mac 默认不推荐本地 AI，因为只能使用 CPU，延迟与发热通常较高。Mac 自动安装会把经过校验的 Ollama 放入当前用户的“应用程序”目录，不需要管理员权限。首次打开时，macOS 仍可能显示系统安全确认或询问是否创建命令行链接；请先核对名称为 Ollama，只按系统提示决定，不要关闭 Gatekeeper。PH Launcher 不依赖系统级命令行链接。macOS 13 可以运行 PH Launcher，但当前 Ollama 需要 macOS 14 或更高版本。

为降低误装风险，一键部署只提供下列已验证模型；其他模型可在高级设置中手动连接：

- 低配且内存至少 8 GB：Qwen3.5 0.8B
- 约 12 GB 内存：Qwen3.5 2B
- 约 20 GB 内存，或至少 16 GB 内存配合 6 GB 级显存：Qwen3.5 4B
- 32 GB 内存、8 GB 级显存且磁盘充足：优先 4B，可自行尝试 9B

这是保守的流畅度建议，不是性能保证；后台程序、散热和显卡共享内存都会影响速度。磁盘不足 6 GB 或内存不足 8 GB 时，PH Launcher 会建议不要进行一键部署。模型文件会占用数 GB 空间，具体大小和下载时间会在页面显示。

不希望由 PH Launcher 自动安装，或已经自行管理 Ollama 时，可展开“手动连接已有 Ollama（高级）”，填写本机地址和已经安装的模型名称。自动部署与手动模式都只连接 `127.0.0.1` 上的本地服务。

选择本地 AI 时，PH Launcher 启动后会在后台准备已安装的 Ollama 与选定模型；默认本机服务未运行时会尝试启动它。自定义本机服务地址仅尝试连接。选择 API 或暂不启用时，不启动或预热本地 AI。不会后台自动安装或下载模型，也不会结束可能被其他软件使用的 Ollama 服务。空闲一段时间后模型可释放占用，下一次提问可能重新加载。

首次模型准备仍可能耗时；普通本地聊天会逐步显示回复，并可点击“停止”。需要读取启动器内容的操作会单独显示状态，写入仍需确认。

从聊天进入连接设置后，页面顶部提供“返回对话”；尚未配置模型时提供“返回首页”。返回不会触发重新部署或新对话。

### API AI

API 模式支持兼容 OpenAI Chat Completions 的 HTTPS 服务。普通对话只发送你主动提交的文字；如果另外开启“AI 操作启动器”，AI 请求读取的任务、课程表或笔记摘要也会发送给你配置的服务商。风险提示会在开启前单独说明。API Key 保存在当前系统用户的加密本地数据中，导出的备份不包含 Key。

请不要把学校账号密码、验证码、他人的个人信息或不应上传的作业材料发送给任何云端 AI。

### AI 操作启动器

AI 连接启用后，可在对话左侧选择“普通对话”“操作前确认”或“完整权限”。操作权限默认关闭，开启前必须阅读并确认风险提示。

“完整权限”经最新风险确认后，允许按请求读取启动器里的课程、成绩、作业、课表、邮件、联系人、日程、笔记、词汇、阅读和专注等学习内容。可以输入“查看我邮箱中关于大学宣讲会的内容并写入日程”，待 AI 搜索、读取后核对生成的清单，再确认写入。缺少明确日期时间时须补充。不会启动后自动把收件箱发给 AI。API 会把请求涉及的内容发送给所选服务商；不希望上传时应选择本地模型。

会话与手动保存的长期记忆在当前系统用户目录单独加密保存。会话最多 30 个、每个 120 条消息，可查看或删除；记忆可编辑或移除。API 模式默认不附加记忆。更换 AI 连接后，旧对话仅可查看，不自动转发给新服务。

邮件中的指令不能授权 AI 操作。系统会尽力隐藏链接与疑似敏感信息，识别为登录、验证码或密码重置通知的邮件不会向 AI 提供正文；这种识别不能保证覆盖所有敏感内容。邮件附件、账号密码和 Cookie 不提供给 AI。更换模型、服务商或连接地址后须重新确认邮件读取权限。

开启后，AI 可以读取经授权的任务、常规课程表、少量笔记摘要、今日概览、离线词典和 IB 指令词，也可以打开启动器页面、学校网站、按名称打开你已添加的网页，或控制专注计时。它不能读取自选网页中的内容或填写网页表单。AI 可以提出添加任务、创建笔记、合并课程和修改任务完成状态，但不会直接写入；每批更改都会显示完整清单，只有用户确认后才保存。

### AI 工作区与外发操作

在 AI 面板的“工作区”中选择或新建一个文件夹后，AI 可以列出该文件夹里的文件、读取文本与 Word 文档，并起草新的 Word 文档或向已有文档追加段落。所有路径都必须落在选定的文件夹内：绝对路径、`..`、符号链接指向的目录和 `.git` 都会被拒绝；没有选择文件夹时文件工具不可用。起草文档同样先生成清单，确认后才会写入磁盘。

完整权限下，AI 还可以提出三类外发操作，全部需要你在清单上确认：用学校邮箱发送邮件、把工作区里的文件提交到 ManageBac 作业、以学生身份回复 ManageBac 讨论。确认后邮件仍会再弹出一次系统确认；提交作业只允许上传工作区内的文件，提交与回复发布后会提示你到学校网站核对结果。未确认时不会发生任何发送、提交或发布。

AI 不会获得凭据库、API Key、工作区之外的目录或任意网页脚本权限，也不能清除登录数据、删除学习资料或修改学校信息。AI 仍可能误解内容，确认前必须核对课程、星期、时间、教室和截止日期。

“从 EduPage 读取常规课表”使用固定的本地只读提取器，只读取课程名、星期、时间和教室。请先在 EduPage 打开“常规课表”，再回到 AI 页面生成预览。PH Launcher 会拒绝把“今日／本周动态课表”中的临时调课、停课或活动导入为每周固定课程；导入只合并，不会因为识别为空而删除原课程。

## 数据与备份

笔记、任务、课程表、日程、词本、阅读与专注记录、设置和 API Key 默认保存在数据目录的 `phl/` 私有文件夹中，并使用系统可用的本地加密能力；Mac 使用钥匙串保护。可在“设置 → 数据与隐私”查看当前数据目录与共用范围。主动启用账号记忆后，学校账号密码会单独使用当前系统用户密钥加密。

数据目录与 [Hello Pinghe! Launcher](https://github.com/huaziqian40-bot/Hello-Pinghe-Launcher) 使用同一套约定：`settings.yaml`（账号与配置）、`Schedule`（按天日程）、`agent/`（AI 会话）三个文件在两个程序共用，各自的私有数据分别放在 `phl/` 与 `phll/`。两个程序安装在同一文件夹并使用便携模式时会自动共用；否则可在“设置 → 数据与隐私”点“改用 Lite 的数据目录”并重启生效。共用目录中的 AI 会话会带 Lite 标记，可以查看；继续聊天会新建属于当前连接的会话，不会把旧内容发给新服务。字段与合并规则见 [数据格式规范](docs/data-format.md)。

“导出备份”生成可迁移的 JSON 文件，不包含 API Key 或保存的学校密码，也不会额外加密。请妥善保管；“恢复备份”会替换当前的本地数据。账号记忆只限三个固定学校站点，不支持自选网站；自动填入不会点击登录。ManageBac 与 EduPage 的自动重新登录需另行明确开启，且不会处理验证码或双重验证。

## 常见问题

- 网页空白或排版异常：刷新后仍有问题时使用“在浏览器中打开”。
- 自选网站无法添加：确认地址以 `https://` 开头，且地址中没有账号或密码。外部单点登录是否能留在启动器内完成，取决于原网站的登录方式。
- 登录循环：清除该网站登录数据后重新登录。清除前确认自己记得账号与密码。
- 自动重新登录没有生效：确认当前站点是 ManageBac 或 EduPage，且已在“设置 → 网站 → 账号记忆”中为该站点明确开启；验证码、双重验证或页面结构变化时请手动登录，不要反复尝试。
- 平和邮箱提示 `ERR.LOGIN.REQCODE`：邮箱连接需要客户端授权码，网页密码本身可能不足以连接。真实收发验证尚未完成。
- 日程没有提醒：打开该日程，填写起止时间并选择提醒；全天事件默认不提醒。确认电脑未休眠且程序仍运行。学校课程提醒则在“我的课表”单独设置，须同步成功并选择自己的教学组。
- 没有课程提醒：确认应用仍在运行或系统托盘／菜单栏中，并检查系统通知权限。
- 本地 AI 无法连接：确认 Ollama 正在运行，并且页面里的模型名称已经安装。
- Mac 显示“无法验证开发者”“文件已损坏”或签名／公证不匹配：停止安装，删除该下载缓存并从正式渠道重新获取；不要使用关闭 Gatekeeper、移除隔离属性或重新签名等绕过方法。
- API 无法连接：核对 HTTPS 地址、模型名称、余额和 Key；不同服务商的兼容程度可能不同。
- EduPage 课表无法识别：确认已经登录并打开“常规课表”，让课程名与具体时间显示在页面上；不要停留在今日动态视图。

学校网站登录后的页面会因账号权限而不同；如某个功能与浏览器中的页面表现不一致，可改用“在浏览器中打开”。

## 1.0 协作预览说明

本版由 PH Launcher（XKRyan）与 Hello Pinghe! Launcher（huaziqian40-bot）开展协作整合，并移植受 GPL-3.0-or-later 约束的学校认证适配代码。没有捆绑 Python 运行时或 Python 依赖，也不代表对方项目的全部功能已移植。真实邮箱收发与模型回答仍待实际使用核对；邮件附件发送、自动标记已读、任意班级及全校空教室查询、不经确认的 AI 写入未开放。完整边界见 [1.0 更新说明](docs/PH-Launcher-1.0-更新说明.md)。
