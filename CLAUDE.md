# PH Launcher — 开发守则

## 核心规则

1. **每次改动后自己验证渲染结果** — 不要只看代码逻辑就交付。用 self-test 或手动检查 DOM 输出，确认页面实际渲染出来的东西是正确的，再交给用户。用户多次因为"代码逻辑看着对但实际渲染不对"而返工，这是不可接受的。

2. **不要用 `AskUserQuestion` 阻塞** — 用户经常离开去睡觉，遇到选择题两个都做。

3. **登录类操作不要自动重试** — ManageBac 会锁定账号。

4. **隐私** — 个人信息（真实姓名拼音、账号密码、班级、邮箱）不能出现在任何公开仓库的输出中。凭据只通过系统加密存储。

5. **小步提交** — 每个功能一个 commit，英文 subject + 中文 body，Co-Authored-By: Claude <noreply@anthropic.com>。

6. **不要推送** — 代码变更后重建测试 exe，用户测试通过后才推送。
